apiVersion: v1
kind: Service
metadata:
  name: {{ include "judge0.fullname" . }}
  labels:
    {{- include "judge0.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "judge0.selectorLabels" . | nindent 4 }}
